{
  "name": "supabase-security-with-schemas",
  "version": "1.0.0",
  "description": "Some baseline defaults and tools for zone-based least-privilege security when using declarative schemas in Supabase.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "pnpm run build",
    "prepublishOnly": "pnpm run build",
    "db": "pnpm run",
    "test": "vitest --no-watch",
    "test:ci": "vitest --no-watch",
    "test:watch": "vitest",
    "start": "supabase start",
    "stop": "supabase stop",
    "destroy": "supabase stop --no-backup",
    "reset": "supabase db reset && pnpm run test",
    "migration": "read -p 'What changed? ' change; [ -z \"$change\" ] && exit 0; supabase db diff -f \"$change\" && echo 'Now run pnpm db reset.'",
    "up": "supabase migration up && pnpm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/pg": "^8.16.0",
    "pg": "^8.16.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "packageManager": "pnpm@10.18.3"
}
